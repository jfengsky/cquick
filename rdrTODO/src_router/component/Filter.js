import React, { Component, PropTypes } from 'react';

class Filter extends Component {
    render() {
        return (
            <div>
                filter
            </div>
        );
    }
}

Filter.propTypes = {

};

export default Filter;